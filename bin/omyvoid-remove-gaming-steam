#!/bin/bash

# omyvoid:summary=Remove Steam and all of its game libraries, configs, and caches.
# omyvoid:requires-sudo=true

set -e

omyvoid-pkg-drop steam

rm -rf \
  "$HOME/.steam" \
  "$HOME/.local/share/Steam" \
  "$HOME/.config/steam" \
  "$HOME/.cache/steam"

echo ""
echo "Steam and its data have been removed."
