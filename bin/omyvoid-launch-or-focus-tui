#!/bin/bash

# omyvoid:summary=Launch a TUI or focus an existing terminal window for it
# omyvoid:args=<command> [args...]

APP_ID="org.omyvoid.$(basename "$1")"
LAUNCH_COMMAND="omyvoid-launch-tui $@"

exec omyvoid-launch-or-focus --bring "$APP_ID" "$LAUNCH_COMMAND"
