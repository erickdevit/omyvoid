#!/bin/bash

# omyvoid:summary=Clear the internal-monitor-disable toggle if no external display is connected.

TOGGLE="$HOME/.local/state/omyvoid/toggles/hypr/internal-monitor-disable.conf"

if [[ -f $TOGGLE ]] && ! omyvoid-hw-external-monitors; then
  rm -f "$TOGGLE"
fi
