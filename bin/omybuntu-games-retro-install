#!/bin/bash

# omybuntu:summary=Create a desktop launcher for a RetroArch game
# omybuntu:args=[core path-to-game]
# omybuntu:examples=omybuntu games retro install snes9x ~/Games/roms/snes/game.sfc | omybuntu-games-retro-install /usr/lib/libretro/mgba_libretro.so ~/Games/roms/gba/game.gba

set -e

source "${OMYBUNTU_PATH:-$HOME/.local/share/omybuntu}/default/i18n/init.sh"

if (( $# == 0 )); then
  mapfile -t cores < <(omybuntu-games-retro-cores)

  if (( ${#cores[@]} == 0 )); then
    omybuntu-notification-send -g "󰯉" "$I18N_GAMES_RETRO_NO_CORES" "/usr/lib/libretro"
    exit 1
  fi

  core=$(omybuntu-menu-select "$I18N_GAMES_RETRO_CORE_PROMPT" "${cores[@]}") || exit 0
  [[ -n $core ]] || exit 0
  core="${core##*(}"
  core="${core%)}"

  game_path=$(omybuntu-menu-file "$I18N_GAMES_RETRO_GAME_PROMPT" "$HOME/Games/roms" "7z bin ccd chd cue dmg elf fds gb gba gbc iso lha m3u md n64 nds nes pbp sfc smc swc zip z64") || exit 0
  [[ -n $game_path ]] || exit 0
elif (( $# == 2 )); then
  core="$1"
  game_path="$2"
else
  echo "Usage: omybuntu-games-retro-install [core path-to-game]"
  echo "Example: omybuntu-games-retro-install snes9x ~/Games/roms/snes/game.sfc"
  exit 1
fi

if [[ ! -f $game_path ]]; then
  echo "Game not found: $game_path"
  exit 1
fi

if [[ $core == */* ]]; then
  core_path="$core"
else
  core_path="/usr/lib/libretro/${core}_libretro.so"
fi

if [[ ! -f $core_path ]]; then
  echo "Core not found: $core_path"
  exit 1
fi

game_name=$(printf '%s' "${game_path##*/}" | sed 's/\.[^.]*$//; s/[[:space:]]*([^)]*)//g; s/[[:space:]]\+/ /g; s/^ //; s/ $//' | perl -Mopen=locale -pe 's/(^|[[:space:]])([^[:space:]])/$1\U$2/g')
desktop_name="$game_name"
desktop_id=$(printf '%s' "$desktop_name" | tr '[:upper:]' '[:lower:]' | tr -cs '[:alnum:]' '-' | sed 's/^-//; s/-$//')
desktop_dir="$HOME/.local/share/applications"
desktop_file="$desktop_dir/$desktop_id.desktop"
icon_dir="$desktop_dir/icons"
icon_path="$icon_dir/Retro Gaming.png"

mkdir -p "$desktop_dir" "$icon_dir"
[[ -f $icon_path ]] || cp "$HOME/.local/share/omybuntu/applications/icons/Retro Gaming.png" "$icon_path"

cat >"$desktop_file" <<EOF
[Desktop Entry]
Version=1.0
Name=$desktop_name
Comment=Play $game_name with RetroArch
Exec=retroarch -L "$core_path" "$game_path"
Terminal=false
Type=Application
Icon=$icon_path
StartupNotify=true
Categories=Game;Emulator;
EOF

chmod +x "$desktop_file"
update-desktop-database "$desktop_dir" &>/dev/null || true

omybuntu-notification-send -g "󰯉" "$(printf "$I18N_GAMES_RETRO_INSTALLED" "$game_name")" "$I18N_GAMES_RETRO_START_INFO"
