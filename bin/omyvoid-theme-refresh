#!/bin/bash

# omyvoid:summary=Refresh the current theme from its templates.

THEME_NAME_PATH="$HOME/.config/omyvoid/current/theme.name"

if [[ -f $THEME_NAME_PATH ]]; then
  OMYVOID_THEME_SKIP_BACKGROUND=1 omyvoid-theme-set "$(cat $THEME_NAME_PATH)"
fi
