#!/bin/bash

# omybuntu:summary=Launch a TUI command in the default terminal with Omybuntu styling
# omybuntu:args=<command> [args...]

exec setsid uwsm-app -- xdg-terminal-exec --app-id=org.omybuntu.$(basename $1) -e "$1" "${@:2}"
