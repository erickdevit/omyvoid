#!/bin/bash

# omyvoid:summary=Returns true if all of the named packages are installed on the system (or false if any of them are missing).
# omyvoid:args=<packages...>

for pkg in "$@"; do
  xbps-query "$pkg" &>/dev/null || exit 1
done

exit 0
