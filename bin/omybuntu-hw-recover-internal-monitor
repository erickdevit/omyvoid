#!/bin/bash

# omybuntu:summary=Clear the internal-monitor-disable toggle if no external display is connected.

TOGGLE="$HOME/.local/state/omybuntu/toggles/hypr/internal-monitor-disable.conf"

if [[ -f $TOGGLE ]] && ! omybuntu-hw-external-monitors; then
  rm -f "$TOGGLE"
fi
