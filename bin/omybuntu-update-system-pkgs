#!/bin/bash

# omybuntu:summary=Update system packages with apt
# omybuntu:requires-sudo=true

set -e

echo -e "\e[32m\nUpdate system packages\e[0m"
sudo apt-get update
sudo DEBIAN_FRONTEND=noninteractive apt-get upgrade -y
