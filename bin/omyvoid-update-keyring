#!/bin/bash

# omyvoid:summary=Ensure the system keyring packages are updated
# omyvoid:requires-sudo=true

echo "Updating system keyrings..."
sudo xbps-install -Sy void-repo-keys ca-certificates
echo "Keys are correct"
exit 0
