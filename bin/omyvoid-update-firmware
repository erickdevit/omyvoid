#!/bin/bash

# omyvoid:summary=Update system firmware using fwupd. Ensures the fwupd EFI binary is installed
# omyvoid:requires-sudo=true

set -e
echo -e "\e[32mUpdate Firmware\e[0m"

if omyvoid-cmd-missing fwupdmgr; then
  omyvoid-pkg-add fwupd
fi

if [[ -d /sys/firmware/efi ]] && [[ -f /usr/lib/fwupd/efi/fwupdx64.efi ]]; then
  sudo install -D /usr/lib/fwupd/efi/fwupdx64.efi /boot/EFI/arch/fwupdx64.efi
fi

fwupdmgr refresh --force
sudo fwupdmgr update
