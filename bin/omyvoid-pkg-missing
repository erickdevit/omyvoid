#!/bin/bash

# omyvoid:summary=Returns true if any of the named packages are missing from the system (or false if they're all there).
# omyvoid:args=<packages...>

for pkg in "$@"; do
  xbps-query "$pkg" &>/dev/null || exit 0
done

exit 1
