#!/bin/bash

# omyvoid:summary=Launch or focus on a given web app identified by the window-pattern.
# omyvoid:args=<window-pattern> <url-and-flags...>

if (($# == 0)); then
  echo "Usage: omyvoid-launch-or-focus-webapp [window-pattern] [url-and-flags...]"
  exit 1
fi

WINDOW_PATTERN="$1"
shift
LAUNCH_COMMAND="omyvoid-launch-webapp $@"

exec omyvoid-launch-or-focus "$WINDOW_PATTERN" "$LAUNCH_COMMAND"
