#!/bin/bash

set -e

source "${OMYVOID_PATH:-$HOME/.local/share/omyvoid}/default/i18n/init.sh"

# Remove this hook after use.
rm -f "$0"

omyvoid-notification-send -g "" -u critical "$I18N_FIRST_RUN_VOXTYPE_TITLE" "$I18N_FIRST_RUN_VOXTYPE_BODY"
