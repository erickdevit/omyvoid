# Gerador de branding

Execute a partir da raiz do repositório:

```bash
python3 tools/branding/generate_assets.py
```

Dependências de build disponíveis no Void Linux: `python3-Pillow` e
`python3-fonttools`. Os arquivos gerados são versionados para que a instalação
do sistema não dependa dessas bibliotecas.
